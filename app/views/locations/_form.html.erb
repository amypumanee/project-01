<%= form_for @location do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :image %>
  <%= f.url_field :image, :required => true %>
  <%= f.label :event_id %>
  <%= f.select :event_id, Event.pluck(:title, :id), :include_blank => true %>
  <%= f.submit %>
<% end %>
